# Dashboard Components

Use this folder for authenticated product surfaces that are reused across dashboard routes.

Good fits:
- Usage summaries
- Trend score panels
- Generated asset previews
- Dashboard tables and filters

Keep route-only state in `app/dashboard/**/page.tsx` until a component is reused.
