# Form Components

Use this folder for reusable form blocks that combine UI primitives with product behavior.

Good fits:
- Caption input groups
- Trend selectors
- Auth form sections
- Copy-to-clipboard controls

Keep low-level controls in `components/ui`.
